# DEPRECATED: Strato Deployment
# Wir nutzen jetzt Vercel - siehe vercel-deploy.yml
# Diese Datei bleibt als Backup erhalten

name: Deploy to Strato (DEPRECATED)

on:
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecated
        run: echo "Strato deployment deprecated. Use Vercel instead."
